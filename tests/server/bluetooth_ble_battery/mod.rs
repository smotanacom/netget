#[cfg(all(test, feature = "bluetooth-ble-battery"))]
mod characteristic_encoding_test;
#[cfg(all(test, feature = "bluetooth-ble-battery"))]
mod e2e_test;
#[cfg(all(test, feature = "bluetooth-ble-battery"))]
mod gatt_examples_test;
